---
title: Configuration
description: Change a service's settings from the dashboard, whenever you need to.
order: 7
---

Each service exposes its settings in the dashboard — whitelists, credentials, limits, whatever that service offers. Changes apply when you save. Changing a service's domain is the one setting that triggers a redeploy.
